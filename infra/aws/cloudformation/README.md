# AWS CloudFormation

> **Status: planejado / não implementado.**

Diretório reservado para os templates CloudFormation de deploy na AWS. Sem arquivos de template por enquanto.

O deploy suportado hoje é **local via Docker Compose** — veja [`infra/docker/README.md`](../../docker/README.md). Alternativa de IaC pretendida ao lado de `terraform/`, cobrindo os mesmos recursos (RDS/pgvector, ElastiCache/Redis, containers e GPU para os provedores de IA).
