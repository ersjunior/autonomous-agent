---
description: Visão geral completa do projeto autonomous-agent
globs: **/*
alwaysApply: true
---

# Autonomous Agent — Visão Geral do Projeto

## O que é este projeto

Sistema multi-agente de IA para atendimento autônomo de clientes em múltiplos canais.
Nome do repositório: `autonomous-agent`
TCC: "Do operador ao Agente: Transformando um atendente de telemarketing em um Agente de Inteligência Artificial Autônomo"

## Stack tecnológica

| Camada | Tecnologia |
|--------|-----------|
| Backend API | FastAPI (Python 3.12) + SQLAlchemy 2.0 async |
| Agentes | LangGraph + LangChain + ProviderFactory (LLM/STT/TTS/Avatar) |
| LLM | OpenAI GPT-4o **ou** Ollama (llama3.1) |
| Frontend | Next.js 15 + TypeScript + Tailwind CSS |
| Workers | Celery + Redis |
| Banco de dados | PostgreSQL 16 + pgvector |
| Cache | Redis 7 |
| STT | OpenAI Whisper **ou** faster-whisper (local) |
| TTS | ElevenLabs **ou** Coqui XTTS-v2 (local) |
| Avatar | D-ID API **ou** SadTalker |
| WhatsApp | Twilio |
| Telegram | python-telegram-bot |
| Infra local | Docker Compose (+ profile `opensource`) |
| Infra cloud | Terraform (AWS + Azure + GCP) |

## Estrutura de pastas (raiz do projeto)

```
autonomous-agent/
├── .cursor/rules/          ← regras do Cursor AI
├── agents/                 ← TODO código de IA (NUNCA em backend/)
│   ├── channels/
│   │   ├── telegram/       ← handler.py ✅
│   │   ├── whatsapp/       ← handler.py + twilio_client.py ✅
│   │   ├── voice/          ← handler.py + tts_stt.py ✅
│   │   └── video/          ← handler.py + avatar_client.py ✅
│   ├── orchestrator/       ← graph.py + router.py ✅
│   ├── providers/            ← LLM, STT, TTS, Avatar (openai, ollama, coqui…) ✅
│   ├── provider_factory.py   ← seleção de provedor via env ✅
│   ├── workers/            ← intent_agent.py + response_agent.py ✅
│   ├── memory/             ← short_term.py + long_term.py ✅
│   └── tools/              ← crm_tool.py + calendar_tool.py + knowledge_base.py
├── backend/                ← FastAPI API apenas
│   └── app/
│       ├── api/v1/         ← auth.py + agents.py + channels.py + leads.py + campaigns.py
│       ├── core/           ← config.py + database.py + security.py ✅
│       ├── models/         ← user.py + agent.py + channel.py + lead.py + campaign.py ✅
│       ├── schemas/        ← user.py ✅
│       └── services/
├── frontend/               ← Next.js dashboard
│   └── src/app/
│       ├── (auth)/         ← login/ + register/
│       └── dashboard/      ← page.tsx + channels/ + agents/ + leads/ + campaigns/ + monitoring/
├── worker/                 ← Celery workers
│   └── tasks/              ← outbound_campaign.py + inbound_handler.py
├── infra/
│   ├── docker/             ← docker-compose.yml ✅
│   ├── aws/terraform/
│   ├── azure/terraform/
│   └── gcp/terraform/
├── docs/
│   └── fine-tuning/        ← guias LLM, STT, TTS ✅
└── tests/
```

## Provedores de IA (ProviderFactory)

Seleção via variáveis de ambiente — implementações em `agents/providers/`:

| Variável | Valores | Padrão |
|----------|---------|--------|
| `LLM_PROVIDER` | `openai` \| `ollama` | `openai` |
| `STT_PROVIDER` | `openai` \| `faster_whisper` | `openai` |
| `TTS_PROVIDER` | `elevenlabs` \| `coqui` | `elevenlabs` |
| `AVATAR_PROVIDER` | `did` \| `sadtalker` | `did` |
| `EMBEDDING_DIMENSIONS` | `1536` (OpenAI) \| `768` (Ollama) | `1536` |

Serviços Docker com `profiles: [opensource]`: `ollama`, `faster-whisper`, `coqui-tts`.
Subir com: `docker compose --profile opensource up -d`.

## Modos de operação do agente

- **ATIVO**: agente inicia contato com leads da base carregada pelo usuário
- **RECEPTIVO**: agente responde clientes que entram em contato

## Serviços e credenciais necessárias

```env
OPENAI_API_KEY         # GPT-4o + Whisper (se LLM/STT provider=openai)
EMBEDDING_DIMENSIONS   # 1536 OpenAI | 768 Ollama
LLM_PROVIDER           # openai | ollama
STT_PROVIDER           # openai | faster_whisper
TTS_PROVIDER           # elevenlabs | coqui
AVATAR_PROVIDER        # did | sadtalker
TWILIO_ACCOUNT_SID     # WhatsApp + Voz
TWILIO_AUTH_TOKEN
TWILIO_PHONE_NUMBER
TELEGRAM_BOT_TOKEN     # Telegram
ELEVENLABS_API_KEY     # TTS (se TTS_PROVIDER=elevenlabs)
ELEVENLABS_VOICE_ID    # ID da voz (default: 21m00Tcm4TlvDq8ikWAM)
DID_API_KEY            # Avatar (se AVATAR_PROVIDER=did)
OLLAMA_BASE_URL        # http://ollama:11434
WHISPER_BASE_URL       # http://faster-whisper:8001
COQUI_BASE_URL         # http://coqui-tts:8002
SADTALKER_BASE_URL     # http://sadtalker:8003
```
