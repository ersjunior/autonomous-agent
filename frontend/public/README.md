# Public

Assets estáticos servidos pelo Next.js diretamente da raiz do site (ex.: `/favicon.ico`, ícones, imagens). Arquivos aqui são públicos e versionados; não coloque segredos nem dados sensíveis.

Referencie-os por caminho absoluto a partir da raiz (ex.: `/logo.svg`).
