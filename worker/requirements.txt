# Worker usa a mesma imagem que o backend (backend/Dockerfile).
# Dependências Python (inclui pypdf + python-docx para KB): backend/requirements.txt
# Rebuild após alterar: docker compose -f infra/docker/docker-compose.yml build worker
celery>=5.4.0
redis>=5.2.0
