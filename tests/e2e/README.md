# E2E Tests

> **Status: planejado / não implementado.**

Ainda não há testes end-to-end automatizados (navegador + stack completa). A validação ponta a ponta hoje é feita manualmente pelo roteiro de smoke test.

- Smoke test manual: [`docs/SMOKE_TEST.md`](../../docs/SMOKE_TEST.md).
- Testes automatizados existentes (unit/integração/API): [`backend/tests/`](../../backend/tests/) — ver [`docs/testes.md`](../../docs/testes.md).

Quando implementado, este diretório deve conter cenários E2E cobrindo os fluxos inbound/outbound dos canais com a stack rodando.
